# Iron burn card — 2026-08-22

## Staged (all in build/rootfs/bin)
| artifact | size | note |
|---|---|---|
| agnos kernel | 1,980,856 B · `148bd34d` | 1.56.46 OPEN + the `#97` PTY-descendant fix; built with agnos's own pin 6.5.28 |
| aethersafha | 4,101,112 B · `a636b517` (0.16.20) | panel producers + frame instrument + `--bandbg` |
| puka | 1,662,888 B | 0.6.20, built with puka's pin 6.5.28 |
| crab / kriya / agnsh | staged | unchanged |

## 1. THE FIX UNDER TEST — does a launched program print in puka?
In puka: `help` then `ls` then `kriya ls /bin`.
- PASS = `ls` lists files.
- FAIL = a prompt and nothing else (the pre-fix symptom).
QEMU already proves it: `ls` glyph px **266 -> 1560**; before the fix `ls`, `ls /`, `kriya ls`
and `kriya ls /bin` ALL rendered an identical 266.

## 2. THE FRAME-BUDGET MEASUREMENT (0.16.18's unpriced regression)
Two runs, IDENTICAL actions, read the `frame cost us THIS WINDOW` lines:
- control (no flag)  -> the shipped full-screen clear
- `--bandbg`         -> the pre-0.16.18 banded clear (⛔ it WILL flicker; that is the point)
Compare `frame` and `clear pct`. This is the number the 0.16.18 trade was made without.

## 3. THE PANEL (never had producers until now)
Look at the top bar: **mem** must show a real %, **cpu** and **disk** must show `--%`
(they have no syscall on agnos and must NOT claim 0%).

## 4. PREVIOUSLY OWED
- **A4/H5** — press a Keychron MEDIA KEY, declared in advance, and watch the cursor. Open since 2026-08-16.
- **`#86` slot ceiling** — the 16-per-boot budget has not been re-counted since the leak was fixed.
- **Theme repaint on the GPU path** — F3 with a client open; QEMU covers only the CPU path.

## ✅ GATE GREEN
`ARC SWEEP: PASS (19 passed, 0 failed)`; `burn-prep.sh` exits 0; iron kernel `148bd34d`.
The 10 GPU FAILs were **not an ABI regression** — `edge-abi-smoke`'s 40 s boot dwell was too short for
the grown battery, so cases that never ran were reported wrong. Dwell is now 180 s.

Flash: `sudo ./scripts/install-media.sh --update-all` (from agnosticos).
⚠ `--update-all`, not `--update` — the oracle here is `run /bin/<tool>`, and an ESP-only refresh
pairs a new kernel with a STALE tool, which fails silently.
