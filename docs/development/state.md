# aethersafha — Current State

> **⛔ 60-LINE CAP. NOT A LOG.** History → [`../../CHANGELOG.md`](../../CHANGELOG.md), milestones →
> [`roadmap.md`](roadmap.md). It once reached 181 lines absorbing release narrative, and carried a version, a
> toolchain pin and eight dep versions all wrong. Over cap = cut prose, never facts. **Refreshed** 2026-08-22.

## TRUE — measured today

| Field | Value | Source |
|---|---|---|
| Version | **0.16.19** (2026-08-22) | [`VERSION`](../../VERSION) |
| Cyrius pin | **6.5.33** — bumped from 6.5.28 at the 0.16.19 cut and the stdlib re-vendored (`cyrius lib sync --full`, 13 files). ⛔ **THE PIN IS NOT DOCUMENTATION** — it selects the sources compiled in. ⛔ **A PINNED SNAPSHOT DIR IS NOT IMMUTABLE — `~/.cyrius/versions/6.5.33/lib` held 6.5.34 content mid-session.** 0.16.19 vendored `bayan` 1.5.2 from it (+215 KB of dead weight) and committed a file its own pin does not provide; corrected here to 1.4.2, and the bump is then only **+4,296 B**. ⇒ 0.16.19's `8ee98f49` **cannot be rebuilt** — history only. **Re-sync and re-measure at every cut; a same-pin re-sync is not a no-op.** ⚠ `lib sync --full` copies the whole snapshot and the build compiles what is in `lib/`, so `[deps].stdlib` does not bound the binary; `CYRIUS_DCE=1` reclaimed nothing. ⛔ **DO NOT CHASE THE PIN — [`CLAUDE.md`](../../CLAUDE.md) Rules.** The wrapper runs ahead and siblings sit on other versions; that is normal, not drift. This repo moved because the operator asked at the 0.16.19 cut | `cyrius.cyml [package].cyrius` |
| Vendored `lib/` | ✅ **In sync with the 6.5.33 pin** — the `ganita` 1.0.4 / `patra` 1.13.0 drift is closed, along with `simd` / `vani` / `yukti`. ⚠ A `lib/`-TRACKED repo, and **`cyrius build` re-vendors it silently**: a "before/after" experiment that rebuilds in between measures the AFTER twice — that invalidated one measurement this session | `cmp` per file vs `~/.cyrius/versions/6.5.33/lib` |
| Modules / tests | 30 `src/*.cyr` · **27 `.tcyr` suites, 1,858 assertions, 0 failed** (`cyrius test` exits 1 on failure — verified, CI gates) | `ls` · `cyrius test` |
| `--agnos` build | **GREEN** — 0.16.19 is **4,337,008 B · `8ee98f495fe90d16`**, rebuilt AT the cut on pin 6.5.33. ⚠ Was 4,121,392 B on 6.5.28; the +215,616 B is the dead `bayan` weight above, not capability. ⛔ **THE 0.16.18 BURN ARTIFACT (`264d1701`) IS HISTORY — it cannot be rebuilt from any current tree.** ⛔ **A SIZE DOES NOT IDENTIFY A BINARY**: 0.16.10/0.16.11 both measured 4,107,920 B under different hashes; 0.16.17/0.16.18 both measured 4,112,704 B. Quote a hash beside every size | `sha256sum` |
| Host (Linux) build | **GREEN**, 25/25 suites (1,778 assertions). ⚠ The host cannot see the defects that matter most here: no GPU means no `#84`, no flip, one buffer — three separate flicker bugs were invisible in QEMU and on the host, and were each settled by an iron burn with a diagnostic flag | measured |
| Backend | **bhumi 1.4.2** — ⭐⭐ **SCANOUT LIVE ON LINUX** (fbdev; bhumi ADR 0003). ⭐⭐ **KEYBOARD + POINTER LIVE on Linux** (evdev, one shared drain; QEMU-proven). GPU via the agnos ring-3 band `#82`-`#94` — **not** mabda; there is no `[deps.mabda]` | `bhumi/src/scanout.cyr` |
| Wallpaper | ⭐ **A FILE** — PNG/JPEG via `[deps.chitra]`, scaled to the screen (0.16.0). ✅ **THE UPLOAD CAP IS GONE — agnos 1.56.44 raised the `#86` slot 2 MB → 32 MB** (region 512 MB). A full-screen wallpaper and any single window fit one slot to **3840×2160**; 5K/8K are the next tier, roadmapped in agnos. ⚠ Still **UNVERIFIED on iron**: QEMU has no AMD PCI device, so every GPU branch is dead there and a burn is the only thing that runs it | measured |
| Substrates | ⭐⭐⭐ **LINUX HOSTS A REAL CLIENT WINDOW, PHOTOGRAPHED IN QEMU** (`scripts/qemu-linux-desktop.sh`; non-black 0.289 vs a 0.011 splash null). **fbdev first, DRM/KMS later.** Sequencing → `roadmap.md` **M6** | measured |

**Deps** — ⭐ **ALL NINE VERIFIED BY A GATE, NOT BY READING (2026-08-21): `scripts/check-dep-tags.sh`, 9/9 clean.** Per dep it checks tag == sibling `VERSION`, the tag exists locally AND on the remote, and every vendored `lib/` file is **byte-identical to that dep's module at that tag** — the check the last three manual sweeps never actually made. Exit 0/1/2; three mutations proven to fail it. ⛔ **LOCAL ONLY** (needs the sibling checkouts; cannot run in CI). Run before every cut and burn. Declared: bhumi 1.4.2 · chitra 0.3.1 · rupa 0.1.4 · agnostik 1.3.5 · agnodrm 1.5.1 · kashi 1.0.6 · mehman 1.0.2 · setu 0.8.7 · **kavach 3.12.2**.
✅ **kavach 3.11.14 → 3.12.2, the 4th `path`-beats-`tag` recurrence, CLOSED.** An ordinary `cyrius build` this session re-materialised `lib/kavach.cyr` from 3.11.15 to the sibling's 3.12.2 (+253/-21) and moved the `cyrius.lock` digest, while the manifest still said 3.11.14. Bumped after verifying all four ways: real tag local + remote (`37f55c2`), clean sibling tree, and the vendored bytes identical to `dist/kavach.cyr` at that tag (`ee243978`). ⭐ **Binary-inert**: `--agnos` is `b033fdfc` / 4,116,816 B before and after, 25/25 green — kavach is a deferred dep and DCE drops it. Siblings: agnos **1.56.46 OPEN** · puka **0.6.19** · crab **0.4.14** · dhancha **0.9.12**.
⛔ **The path WINS over the tag** — the vendored copy tracks the local checkout whatever the tag says, so a green build here does not prove the declared graph builds. **Four recurrences; three manifest comments have not stopped one of them, because the rewrite happens at BUILD time and warns nobody.** ⇒ It wants a gate that diffs every declared tag against its sibling `VERSION` at check time — a comment cannot fix a mechanism.

## Proven, and by what

**Every `AE-` rung is shipped and iron-proven** (ladder → [`planning/desktop.md`](planning/desktop.md)). On archaemenid, on the FRAMEBUFFER not the serial: `--selftest` → **exit 95**; **two real clients**; a live agnoshi in puka's window answering typed keys; a **titlebar drag**; a GPU cursor (`#92` op 0x03); `AE-9` — every frame layer on the GPU.
⛔ Three needed a defect found by AUDIT, not a burn: the handshake must **drain** a channel dropping the OLDEST record and **resync**; `hid_mouse_take` re-seeded `buttons_seen` to the held LEVEL, making the drag-release edge unreachable; the seeded "Files" window had no client fd and **swallowed keys focused onto it** — that WAS the "puka got no keys" report.
⭐⭐ **M6-C3 DONE, IRON-PROVEN (2026-08-16, agnos 1.56.45).** A see-through `crab` over a working `puka` terminal, chrome ON the GPU: `#92` **op 0x06** for the client surface, **op 0x02 BLEND_COV** with a constant-coverage mask for the alpha chrome. ⛔ Three burns, all one missing primitive — `fill_rect_a` had no GPU route. op 0x02 had been advertised since it was `#93`. ⇒ **Check the op list before calling a gap structural.** ⚠ Unsettled: the rounding tie (agnos owns it) and a measured OVERLAP.
⚠ **`#86` SLOT LEAK — CAUSE FIXED, CEILING UNRE-MEASURED.** The budget fell **16 → 15 → 14 → 13**, one per compositor run: setu's `present_probe` ignored `SETU_CLOSE` and survived holding its buffer. setu 0.8.6 fixed the probe and crab 0.4.12 now exits on EOF too. ⛔ The 16-per-boot ceiling has NOT been re-measured since — treat it as open until a burn counts it again.
⭐⭐ **2026-08-18 BURN — M7 SHIPPED AND TWO DEFECTS FOUND.** Launcher (F2), LIST panes, clipping, both event shapes and puka's widget tree all reached iron. ⭐ **puka renders OPAQUE** — 0.6.15's alpha fix holds, invisible off-target by construction. Fixed after: the compositor acted on BOTH key edges (three F3 presses → six theme switches), and **maximize faulted the desktop** (`cr2=0x12600000`, exit 142).
⛔ **ONE ROOT CAUSE FOR TWO REPORTS.** The GPU blit took its extent from the WINDOW while the client's buffer stayed its own size, and `ae_gpu_window_admissible` compared the window only to the SCREEN — which a maximized window passes trivially. `win_resize_by` diverges identically: that was the "drag destroyed crab's FB" report. Fixed in 0.16.8 — record the attached size (`win_set_client_buf` carries it), refuse/clamp an unbacked window, and SEND `SETU_CONFIGURE`, a message that sat in setu's protocol with **zero senders and zero handlers**. Verified in QEMU: `agnos/scripts/harness/ae-resize-fault-test.py`, `SEQ=f5`.

## Open

> ⛔ **CLOSED — a falsified premise left under "Open" gets re-derived as work.** ~~`#92` premultiplied never runs~~ · ~~`#88`/op 0x03 unconsumed~~ · ~~no pointer on iron~~ · ~~damage blit needs `union(cur, prev)`~~. ⚠ **CORRECTED 2026-08-12**: `#89` +0..+28 and `#90` all have live consumers; `#91` and batched `#92` are **falsified, not pending**.

- 🔴🔴 **THE NEXT WORK — THE FLICKER FIX MADE THE DESKTOP SLOW. INSTRUMENT SHIPPED; THE BURN IS OWED.**
  Operator, on iron after 0.16.18: the whole-screen refresh is **slower**. ⛔ **Reported at the 0.16.18 burn and DROPPED from the handoff.** The CHANGELOG banked *"`--clearbg` … not reported as slow"* and shipped on it; the operator's later verdict overrides that.
  **Mechanism:** `desk_bg_clear_is_full` ([`desktop.cyr`](../../src/desktop.cyr)) returns 1 whenever `ae_chrome_on_gpu()` — which since **AE-9 is EVERY iron frame** — so the clear is full screen, forever, on the only path that matters. **SOLID** = one `#88` over 1440 rows vs the iron band's steady **h=414**; **gradient** = `rend_clear_band` **PER ROW**, 1440 rects/frame; **IMAGE** without the `#87` upload = a full-screen `bhumi_fb_set` loop (**3.7 M px**) the deleted `#39` blit then **DISCARDS**. AE-0a's **3.83 → 2.46 ms of a 6.40 ms frame** handed back.
  ⛔⛔ **AND IT WAS NEVER MEASURABLE — `ae_now_ms` WAS FROZEN ON IRON. FOUND + FIXED 2026-08-21.** It read `sys_uptime_ms` (#40 → `timer_ticks`, incremented by the 100 Hz timer ISR), but the desktop starts as `run /bin/aethersafha` — a FOREGROUND program, **IF CLEARED**; only `/bin/agnsh` gets IF=1 (agnos `kernel/core/main.cyr:4241`). The timer never fires, so **#40 is frozen for the compositor's whole run** and every agnos timing readout was a constant. Same class as the host arm's `return 0` (B1, 0.13.1), on the target that matters, and it is *why* "nobody measured the frame cost" — nobody could. The same mistake cost agnos two burns on its rung-10 gate. ⇒ Now `#95 uptime_us` (rdtsc, needs no interrupts), with its **-1 "calibration refused" propagated, never floored to a plausible 0**.
  ⭐ **[`src/frametime.cyr`](../../src/frametime.cyr) + [`tests/frametime.tcyr`](../../tests/frametime.tcyr)** — per-window frame/clear cost, bounded to 12 report lines. **48 assertions, 9 mutations proven to fail it.** ⚠ Reports **windows, not cumulative means**: the first cut decayed 47→24→16→12 µs with the workload unchanged, which would have confounded two burns by RUN LENGTH.
  ⭐ **`--bandbg`** forces the banded clear back on the GPU path — reintroducing the flicker on purpose — so ONE burn yields both numbers under identical actions. Same shape as `--fullclear` → `--clearbg`.
  📊 **HOST, CPU PATH ONLY (2026-08-21)**: banded frame **1234–1623 µs**, clear ~**1 µs**; `--clearbg` frame **6386–6410 µs**, clear **5132–5165 µs** = **80% of the frame**. ⇒ **~4× on the CPU path.** ⛔ **THIS IS NOT THE IRON NUMBER AND MUST NOT BE QUOTED AS ONE** — on iron the clear is a `#88` CP-DMA fill, not a CPU store loop, and the host has no client so its band is empty (the degenerate best case). The iron figure is **still unmeasured**; that is what the burn is for.
  ⇒ Then the durable fix is **per-buffer damage** — which rows are stale in *this* half of the flip pair — not a blunt full clear. `--fullclear`/`--clearbg`/`--bandbg` all stay.
- 🟡 **OPEN, NOT REPRODUCED — theme repaint on the GPU path.** 2026-08-18: *"theme switching works for the titlebar but not background or desktop top bar."* QEMU per-region: **top bar 55/55, background 39/39 change — both repaint**, but QEMU has no AMD device so that covers only the CPU path. ⭐ Likeliest the SAME defect as the 0.16.9 flashing. **Re-test before treating it as separate.** ⚠ The 0.16.9 launcher-damage fix is **NOT unit-covered** — a discriminating test needs a live damage tracker only `main.cyr` allocates, and a mutation removing the call PASSED the check written for it.
- 🔴 **agnos 1.56.46 is OPEN**, carrying two kernel defects: the **`VFS_CHAN` close leak** (`vfs_close_inner` has an arm for every tag but this one; endpoints hold until process death) and the **1.56.41 keystroke loss** (0/9, 4/9, 4/9 at a ~100 ms hold — ⛔ its recorded cause is contradicted by the code: `hid_poll` also runs from the timer ISR and the xHCI MSI-X handler, so it is **unexplained**).
- **Then, in handoff order**: present-protocol damage rects (a maximized 2560×1408 terminal moves ~28.8 MB/frame vs 983 KB at 80×24 — setu has **no damage concept at all**, kinds 1–11); no TIOCSWINSZ on the `#97` band; ✅ **panel producers DONE** ([`sysprobe.cyr`](../../src/sysprobe.cyr) — mem real & oracle-checked vs `free -b`; cpu/disk have no syscall on agnos and render `--%`; ⚠ **not yet pixel-proven**, `--ppm` is still unbuilt); crab has no maximize view; agnos aarch64 has 30 reachable-undefined fns; C4a's present half undecided.
- **`apps.cyr`/`screen_capture.cyr`/`screen_recording.cyr` are in NO build graph** — `main.cyr` never includes them; 2,023 lines reachable only from `tests/`. ⚠ **A compositor that WEDGES still writes no log** — the `/klug.txt` spill hangs off scanout-owner *exit*, so it covers a crash and a quit but not a hang.

## Pointers

Milestones → [`roadmap.md`](roadmap.md) · desktop arc → [`planning/desktop.md`](planning/desktop.md) · **handoff →
[`desktop-arc-handoff.md`](desktop-arc-handoff.md)** · history → [`../../CHANGELOG.md`](../../CHANGELOG.md) · parity
→ [`parity-plan.md`](parity-plan.md) (⚠ stale: headed "post-0.1.0") · GPU band → agnos `agnos-userland-abi.md`.
